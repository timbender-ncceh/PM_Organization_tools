# secret_ical_url.R

# set google calendar ical urls (secret) for PM calendar and MTG calendar below.  
# stand-alone so can be gitignored

secret.ical.url_PM <- "<secret_ical_url>"
secret.ical.url_MTG <- "<secret_ical_url>"
